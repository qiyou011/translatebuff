---
"@read-frog/extension": patch
---

fix(options): align shared dialogs and side sheets, component hover, focus, selection, validation and disabled states, webpage translation, menus, custom actions and detail pages with Figma, including subtitle settings, editor layouts and localized field labels, while preserving existing interactions

fix(ui): keep text inputs, textareas and grouped color fields visually unchanged on hover and focus across extension surfaces

fix(ui): use translucent dialog backdrops so the current page remains visible behind modal dialogs

fix(options): size select menus to their contents and wrap long provider names within the viewport
