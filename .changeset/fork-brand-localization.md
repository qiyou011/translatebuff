---
"@read-frog/extension": patch
---

fix(fork): localize provider UI across nine languages and show edition-aware branding with model-only translation card labels

fix(fork): localize static page and subtitle previews by UI language while preserving session-only custom preview drafts
