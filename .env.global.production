# ⚠️ 本文件已被强制跟踪、随仓库公开（qiyou011/translatebuff 是 public 仓库），
#    且会被打进 Firefox 的 sources 包上传到 GitHub Release。
#    .gitignore 里虽有 `.env.*`，但本文件是 force-add 进来的，看起来被忽略、实际没有。
#
#    只放「客户端本来就能拿到」的值：后端地址、OAuth client id、分析 ingest key 之类，
#    这些编进扩展产物后任何用户解包即可读取，公开无损失。
#
#    禁止放服务端密钥（API secret / 私钥 / 数据库口令 / 管理凭据）。
#    需要这类值时走服务端下发，扩展侧只读、不持有——见 FORK_GUIDE.md「门禁/权限全放服务端」。
#
# ===== 海外线（edition=global）正式配置 =====
# 由 `node scripts/pack.mjs store --edition global ...` 注入，优先级高于 .env.production。
# 本文件里出现的域名会被 assert-fork-build 当作「海外包必须含、国内包绝不能含」的双向断言依据。

# 官网域取实际部署的 www host：裸域 translatebuff.com 是一台只做 301 的 nginx，
# 而官网写 Login-Credential cookie 不带 domain（host-only），配裸域会让冷启动读 cookie 与登出删 cookie 落空。
WXT_API_URL=https://www.translatebuff.com
WXT_WEBSITE_URL=https://www.translatebuff.com
WXT_OFFICIAL_SITE_ORIGINS=https://translatebuff.com,https://www.translatebuff.com
WXT_AUTH_COOKIE_DOMAINS=translatebuff.com
WXT_GOOGLE_CLIENT_ID=REPLACE_WITH_FORK_GOOGLE_CLIENT_ID
WXT_POSTHOG_HOST=https://us.i.posthog.com
WXT_POSTHOG_API_KEY=REPLACE_WITH_FORK_POSTHOG_KEY
# 兼容旧平台 API 配置：暂保留原占位值，不代表后端已确认此域可用。
# 海外会员流程已分别走 AUTH_BFF / CLAW，不直接使用此项；构建护栏仍要求非空。
WXT_RENYIMIAO_API_URL=https://www.translatebuff.com
# 翻译网关回退地址：后端本次未提供，暂保留原占位值，发布前仍需确认（MUL-67）。
# 登录后优先使用 /api/claw_bff/v1/tokens 返回的 base_url；不可由业务 API 域推断网关地址。
WXT_RENYIMIAO_GATEWAY_URL=https://www.translatebuff.com/v1

# 海外登录 BFF：后端已确认的正式域，不再假设走官网同源反代。
# 只填 HTTPS 域名，代码自行拼接 /api/login_registration_bff/ 下的路径。
WXT_RENYIMIAO_AUTH_BFF_URL=https://lrbff-us.htjsq.com

# 海外会员额度、令牌接口：后端已确认的正式域。
# 只填 HTTPS 域名，代码自行拼接 /api/claw_bff/ 下的路径。
WXT_RENYIMIAO_CLAW_API_URL=https://api.translatebuff.com

# 海外埋点域：后端已确认用于 /api/data_report/ 下的卸载问卷上报。
# 插件另有 translate_active 活跃上报，发布前需验证 /api/data_report/v1/client/click_event 也受支持。
WXT_REPORT_API_URL=https://tob.6fast.com
