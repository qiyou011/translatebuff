# 线上浏览器实测：免费线路首轮

时间：2026-09-12 14:25–14:43（Asia/Taipei）。用户明确授权使用当前浏览器及线上会员账号。未调用付费LLM；未改动业务代码。

## 环境与边界

- 使用用户现有 Google Chrome 配置，通过真实Chrome界面、扩展popup和Service Worker DevTools操作；不是模拟浏览器或mock请求。
- 已核验并重新加载未打包扩展：`translatebuff/.output/chrome-mv3`，ID `jjgoechanghiknlblangmfijblhggkdl`。显示版本仍为任译喵1.3.0（rf1.46.6），内容为本次候选实现，非已发布1.4.0。
- 实际配置：页面批量16/4000，请求rate=8、capacity=20；bilingual、range=all、preload margin=1000/threshold=0，默认提示、Google提供商。
- 浏览器连接接口不可用，改用原生Chrome界面。未启用远程调试权限、未更改代理或安全设置。
- 现有配置用于用户指定的登录环境；未清空既有缓存、未修改提供商/语言/批量配置。不能视为独立干净profile的完整基准。

## 真实页面结果

页面：<https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions>。

通过页面悬浮球触发Google翻译，观察到标题“函数 - JavaScript | MDN”、正文和侧栏真实中文双语译文，保留了原始浏览器截图于当前任务工具记录。

后台临时计量观察到：

| 请求     | 项数 | 请求体字节 | HTTP | 响应项数 | 网络响应体完成耗时 |
| -------- | ---: | ---------: | ---: | -------: | -----------------: |
| Google 1 |    1 |         57 |  200 |        1 |             228 ms |
| Google 2 |   36 |        683 |  200 |       36 |             274 ms |
| Google 3 |   23 |       2120 |  200 |       23 |             172 ms |

本轮捕获67条真实页面取文消息、2658个UTF-16字符，format均为plain；网络在途观察峰值3，结束时0。现有缓存和去重可能影响请求数，未据此计算整页降本比例。只捕获初始视口及配置预加载范围，不是整页冻结样本。

## 同源快照强制绕缓存回放

将上述67条公开取文通过真实扩展 `enqueueTranslateRequest` 消息入口回放，使用独立测试hash及 `forceRetranslation=true`，来源eng、目标cmn；保持原后台工厂、批量和队列，不直接调用mock适配器。不修改用户持久配置、不清除旧缓存。

| 路线      | 最终成功 | 实际网络请求            | 端到端消息耗时 | 说明                                                        |
| --------- | -------- | ----------------------- | -------------: | ----------------------------------------------------------- |
| Google    | 67/67    | 2次，每次67条、2893字节 |        1594 ms | 首次167 ms网络错误；队列重试后200，67条响应，成功尝试226 ms |
| Microsoft | 67/67    | 1次，67条、2870字节     |        2306 ms | 200，67条响应，网络响应体完成2154 ms                        |

两条路线顺序运行，观察峰值均不超过1，无未完成请求；无返回错误或缺项。样本译文包括Google的“函数 - JavaScript | MDN”“跳至内容”，Microsoft的“函数 - JavaScript |MDN”“跳转到主要内容”。

Google的失败尝试纳入请求数和总耗时，不能只计成功的一次。上述是一次小规模运行，不是多次median；未与旧版本单条请求执行同口径基准，因此不宣称≥90%请求下降或卡顿已解决。回放不等价于真实DOM回填性能测试；仅页面首次点击部分包含DOM证据。

## 冻结数据

- 仓内快照：`fixtures/mdn-viewport-2026-09-12.json`，67条公开内容，不含账号、凭据或请求头。
- 仓内文件SHA-256：`739fb5ee346c4894ffa9cf9c05ded05b9efd954f7531add3a3c987343b51472f`。
- 浏览器原始导出SHA-256：`cb2563d8b6bba4feb969649441f35ea240709771d51b1a99c4eeb646f8eadb11`；仓内文件仅增加末尾换行，JSON内容一致。
- 原始导出位于用户Downloads的 `v140-mdn-viewport-snapshot.json`。

## 会员会话阻断

后续状态：用户已完成正式登录，此阻断解除；会员LLM续测结果见 `browser-llm-2026-09-12.md`。以下保留首轮历史事实。

重载前popup曾显示PRO。加载本次生产构建后，popup变为“登录”；保存的会员模型baseURL仍为测试网关，key布尔检查为空。点击登录后打开正式站点 `https://translatebuff.cn/zh-hans/login`，页面要求手机号及验证码，没有可自动沿用的线上登录态。

没有捕获导致旧会话失效的那次认证网络响应，因此不认定具体401原因，也不擅自改写会员模型配置。已请用户在网页完成登录，验证码不进入聊天。未触发发送验证码或提交协议。

LLM token、六类模型、HTML紧凑协议、三页全量样本、并发2/4/6及交互延迟仍未验收，tasks 5.1–5.4保持未完成，默认值不冻结。

## 清理与副作用

临时fetch包装已还原、runtime监听已移除并检查成功，session临时公开快照已删除，测试全局变量已移除，监控窗口已关闭。未清空用户缓存；强制回放产生独立 `v140-live-*` 测试缓存条目。保留公开快照与测试页面，浏览器最终聚焦线上登录页。未提交、推送、归档或发版。
