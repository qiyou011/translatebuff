# 2026-09-20 复核记录

## 修复范围

根据后续 Figma 节点 118:216、119:624、119:825 和 119:654，恢复未锁定悬浮按钮贴边收起，按明暗主题切换正式图标资源，修复工具按钮表面、关闭菜单尺寸、圆角、字号及浅色悬停色。

浅色悬停色为 #E6E6E9。设计节点没有独立深色悬停示例，深色悬停采用 #27272A；该值属于实现选择，不作为设计稿已核实属性。

除恢复贴边收起的展示条件外，未修改翻译、设置、拖拽、锁定、禁用操作的处理逻辑或配置结构。

## 验证结果

- 真实 Chrome 加载构建产物：收起可见宽度 20px、透明度 40%，展开宽度 40px；主题资源切换通过。
- 菜单计算尺寸 128×80；选项 116×34、8px 圆角、12px 字号和 22px 行高；浅色悬停色验证通过。
- 浮动按钮专用测试：22/22 通过。
- 全量测试：3700 通过，4 跳过；按项目要求设置 SKIP_FREE_API=true。首次并发运行发生超时，失败文件单独复跑及完整测试复跑均通过。
- 构建、lint、格式检查及 git diff --check 通过。
- GitNexus 变更分析识别到浮动按钮流程，风险 medium；索引落后 4 个提交，已结合实际差异检查范围。

用户本次明确要求提交并归档，覆盖原规划中不自动提交及归档的交付约束。
