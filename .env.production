# ⚠️ 本文件已被强制跟踪、随仓库公开（qiyou011/translatebuff 是 public 仓库），
#    且会被打进 Firefox 的 sources 包上传到 GitHub Release。
#    .gitignore 里虽有 `.env.*`，但本文件是 force-add 进来的，看起来被忽略、实际没有。
#
#    只放「客户端本来就能拿到」的值：后端地址、OAuth client id、分析 ingest key 之类，
#    这些编进扩展产物后任何用户解包即可读取，公开无损失。
#
#    禁止放服务端密钥（API secret / 私钥 / 数据库口令 / 管理凭据）。
#    需要这类值时走服务端下发，扩展侧只读、不持有——见 FORK_GUIDE.md「门禁/权限全放服务端」。

WXT_API_URL=https://translatebuff.cn
WXT_WEBSITE_URL=https://translatebuff.cn
WXT_OFFICIAL_SITE_ORIGINS=https://translatebuff.cn,https://www.translatebuff.cn
WXT_AUTH_COOKIE_DOMAINS=translatebuff.cn
WXT_GOOGLE_CLIENT_ID=REPLACE_WITH_FORK_GOOGLE_CLIENT_ID
WXT_POSTHOG_HOST=https://us.i.posthog.com
WXT_POSTHOG_API_KEY=REPLACE_WITH_FORK_POSTHOG_KEY
# 任译喵登录后端域（平台 common_bll/claw_bff）——正式环境真值。common 与 claw 同 host、仅 path 不同。
# 注意：登录后端域 ≠ 翻译网关域（WXT_RENYIMIAO_GATEWAY_URL），两个不同域、不同性质、勿混。
WXT_RENYIMIAO_API_URL=https://translatebuff.cn
# 任译喵翻译网关域（oneapi，登录前默认 / base_url 缺失回落；真实网关登录后由 /v1/tokens 动态下发）。
WXT_RENYIMIAO_GATEWAY_URL=https://translatebuff.cn/v1
